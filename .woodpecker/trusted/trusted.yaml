# Faktor CI — trusted workflow: push (any branch) + version tags.
#
# TRUST BOUNDARY: this file lives at the pipeline path of the TRUSTED
# Woodpecker project (server-side: trusted.volumes=true, allow_pr=false).
# The pipeline path is a project setting; a PR diff cannot move the trusted
# project onto its own YAML, and the untrusted PR project (trusted=false)
# cannot mount volumes at policy level. See scripts/woodpecker/setup.md §5.
#
# This is the only workflow file set that declares named volumes. The
# untrusted pull_request event runs `.woodpecker/untrusted/pr.yaml`, which
# declares none, so a PR pipeline can never mount (or write into) the
# `faktor-trusted-*` caches. The cron workflow
# (`.woodpecker/trusted/nightly.yaml`) uses its own `faktor-nightly-*`
# volumes so heavy campaigns cannot poison these caches either.
#
# Job graph (one workflow per matrix platform; all mandatory linux lanes run
# as parallel steps of a single workflow so they share one workspace):
#
#   stage 1 (parallel): linux · static · docs · vscode · jetbrains-build
#                       · jetbrains-smoke
#   stage 2:            perf (after linux, static, docs)
#   stage 3:            certificate (after every linux lane)
#
#   darwin combo:  darwin-check · darwin-test · darwin-doctor -> certificate-darwin
#   windows combo: windows-check · windows-test -> certificate-windows
#
# Aggregation mechanism (the one Woodpecker actually supports):
#   * Woodpecker workflows are filesystem-isolated, so markers only work
#     between steps of ONE workflow (docs: "files are only shared between
#     steps of the same workflow"). Every lane therefore ends by writing
#     target/certification/lanes/<lane>.json with its lane name, commit and
#     status; the certificate step depends_on every lane, runs with
#     `when.status: [success, failure]` (dependents are otherwise skipped
#     when a dependency fails) and fails when any marker is missing, marked
#     failed, carries a foreign commit, or is not the expected schema.
#   * CI_PIPELINE_STATUS is the runtime's own "workflow failed before this
#     step" signal and is checked as an independent second condition.
#   * The per-platform certificate steps cover the self-hosted darwin and
#     windows agents; cross-platform lanes cannot be folded into the linux
#     certificate because workflows share no filesystem.
#
# Agent routing: `labels.platform` is the documented agent selector. The
# deprecated `runs_on` key is NOT an agent selector in 3.x (it feeds the
# run-on-success/failure flag of dependent tasks and would make a workflow
# never match); see scripts/woodpecker/setup.md.
#
# Named volumes (cargo/gradle/npm/playwright caches) require the trusted
# PROJECT (not just the repository) to be marked Trusted (volumes) by a
# server admin; delete the `volumes:` lines if your instance cannot grant
# that. No secrets are required.
#
# Bootstrap audit: the JetBrains smoke toolchain is installed from the pinned
# rustup-init binary after verifying its published SHA-256; no step pipes a
# remote script into a shell.
#
# Commit-status contexts (default `WOODPECKER_STATUS_CONTEXT_FORMAT`):
#   push: ci/woodpecker/push/trusted   tag: ci/woodpecker/tag/trusted

matrix:
  platform:
    - linux/amd64
    - darwin/arm64
    - windows/amd64

labels:
  platform: ${platform}

# push (any branch) and tag events are collaborator-authored: only these may
# mount the trusted caches. pull_request never reaches this file.
when:
  - event: [push, tag]
    evaluate: 'platform == "linux/amd64"'
  - event: push
    branch: main
    evaluate: 'platform != "linux/amd64"'

steps:
  # ---------------------------------------------------------------- linux --
  # The default lane (hosted linux/amd64 runners). Same commands the CI
  # gates always ran: fmt + clippy -D warnings + check/test --all-features +
  # doctor. Protocol/stream tests ride the --workspace all-features run.
  # Long lanes stay #[ignore]-gated.
  - name: linux
    image: rust:1.98
    when:
      matrix:
        platform: linux/amd64
    environment:
      RUST_BACKTRACE: "1"
      CARGO_TARGET_DIR: /woodpecker-cache/target
    volumes:
      - faktor-trusted-cargo-registry:/usr/local/cargo/registry
      - faktor-trusted-cargo-git:/usr/local/cargo/git
      - faktor-trusted-target-linux:/woodpecker-cache/target
    commands:
      - apt-get update && apt-get install -y --no-install-recommends python3 procps ca-certificates
      - rustup component add rustfmt clippy
      - cargo fmt --check
      - cargo check --workspace --all-features
      - cargo clippy --workspace --all-targets --all-features -- -D warnings
      - cargo test --workspace --all-features
      - cargo run -p faktor-cli -- doctor --data-dir /tmp/kp-ci
      - |
        set -eu
        mkdir -p target/certification/lanes
        CMDS="$(cat <<'CMDS'
        apt-get update && apt-get install -y --no-install-recommends python3 procps ca-certificates
        rustup component add rustfmt clippy
        cargo fmt --check
        cargo check --workspace --all-features
        cargo clippy --workspace --all-targets --all-features -- -D warnings
        cargo test --workspace --all-features
        cargo run -p faktor-cli -- doctor --data-dir /tmp/kp-ci
        CMDS
        )"
        cmds_digest="sha256:$(printf '%s' "$CMDS" | sha256sum | cut -d' ' -f1)"
        cmds_b64="$(printf '%s' "$CMDS" | base64 | tr -d '\n')"
        iso_ts() { t="$1"; if [ -n "$t" ]; then date -u -r "$t" +%Y-%m-%dT%H:%M:%SZ 2>/dev/null || date -u -d "@$t" +%Y-%m-%dT%H:%M:%SZ 2>/dev/null || date -u +%Y-%m-%dT%H:%M:%SZ; else date -u +%Y-%m-%dT%H:%M:%SZ; fi; }
        started_at="$(iso_ts "${CI_PIPELINE_STARTED:-}")"
        finished_at="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
        tree="$(git rev-parse 'HEAD^{tree}')"
        artifacts="[]"
        artifact_digest="e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855"
        printf '{"schema":"faktor-woodpecker-lane/v2","lane":"linux","status":"passed","commit":"%s","tree":"%s","runner":{"os":"%s","arch":"%s","ci":"woodpecker","run_id":"%s"},"started_at":"%s","finished_at":"%s","commands_b64":"%s","commands_digest":"sha256:%s","artifacts":%s,"artifact_digest":"sha256:%s"}\n' \
          "$CI_COMMIT_SHA" "$tree" "$(uname -s | tr '[:upper:]' '[:lower:]')" "$(uname -m)" "${CI_PIPELINE_NUMBER:-0}" "$started_at" "$finished_at" "$cmds_b64" "$cmds_digest" "$artifacts" "$artifact_digest" > target/certification/lanes/linux.json
  - name: static
    image: rust:1.98
    when:
      matrix:
        platform: linux/amd64
    environment:
      RUST_BACKTRACE: "1"
      CARGO_TARGET_DIR: /woodpecker-cache/target
    volumes:
      - faktor-trusted-cargo-registry:/usr/local/cargo/registry
      - faktor-trusted-cargo-git:/usr/local/cargo/git
      - faktor-trusted-target-static:/woodpecker-cache/target
    commands:
      - cargo test -p faktor-tests-static-authority
      - cargo test -p faktor-security
      - cargo test -p faktor-tests-fuzz-seeds
      - SUPPLY_CHAIN_BUILD=1 SUPPLY_CHAIN_ARTIFACTS="$CARGO_TARGET_DIR/release/faktor-cli" bash scripts/supply-chain.sh
      - |
        set -eu
        mkdir -p target/certification/lanes
        CMDS="$(cat <<'CMDS'
        cargo test -p faktor-tests-static-authority
        cargo test -p faktor-security
        cargo test -p faktor-tests-fuzz-seeds
        SUPPLY_CHAIN_BUILD=1 SUPPLY_CHAIN_ARTIFACTS="$CARGO_TARGET_DIR/release/faktor-cli" bash scripts/supply-chain.sh
        CMDS
        )"
        cmds_digest="sha256:$(printf '%s' "$CMDS" | sha256sum | cut -d' ' -f1)"
        cmds_b64="$(printf '%s' "$CMDS" | base64 | tr -d '\n')"
        iso_ts() { t="$1"; if [ -n "$t" ]; then date -u -r "$t" +%Y-%m-%dT%H:%M:%SZ 2>/dev/null || date -u -d "@$t" +%Y-%m-%dT%H:%M:%SZ 2>/dev/null || date -u +%Y-%m-%dT%H:%M:%SZ; else date -u +%Y-%m-%dT%H:%M:%SZ; fi; }
        started_at="$(iso_ts "${CI_PIPELINE_STARTED:-}")"
        finished_at="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
        tree="$(git rev-parse 'HEAD^{tree}')"
        artifacts="[]"
        artifact_digest="e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855"
        printf '{"schema":"faktor-woodpecker-lane/v2","lane":"static","status":"passed","commit":"%s","tree":"%s","runner":{"os":"%s","arch":"%s","ci":"woodpecker","run_id":"%s"},"started_at":"%s","finished_at":"%s","commands_b64":"%s","commands_digest":"sha256:%s","artifacts":%s,"artifact_digest":"sha256:%s"}\n' \
          "$CI_COMMIT_SHA" "$tree" "$(uname -s | tr '[:upper:]' '[:lower:]')" "$(uname -m)" "${CI_PIPELINE_NUMBER:-0}" "$started_at" "$finished_at" "$cmds_b64" "$cmds_digest" "$artifacts" "$artifact_digest" > target/certification/lanes/static.json
  - name: docs
    image: rust:1.98
    when:
      matrix:
        platform: linux/amd64
    environment:
      RUST_BACKTRACE: "1"
      CARGO_TARGET_DIR: /woodpecker-cache/target
    volumes:
      - faktor-trusted-cargo-registry:/usr/local/cargo/registry
      - faktor-trusted-cargo-git:/usr/local/cargo/git
      - faktor-trusted-target-docs:/woodpecker-cache/target
    commands:
      - cargo doc --workspace --all-features --no-deps
      - bash scripts/branding-scan.sh
      - bash scripts/check-docs-sync.sh
      - |
        set -eu
        mkdir -p target/certification/lanes
        CMDS="$(cat <<'CMDS'
        cargo doc --workspace --all-features --no-deps
        bash scripts/branding-scan.sh
        bash scripts/check-docs-sync.sh
        CMDS
        )"
        cmds_digest="sha256:$(printf '%s' "$CMDS" | sha256sum | cut -d' ' -f1)"
        cmds_b64="$(printf '%s' "$CMDS" | base64 | tr -d '\n')"
        iso_ts() { t="$1"; if [ -n "$t" ]; then date -u -r "$t" +%Y-%m-%dT%H:%M:%SZ 2>/dev/null || date -u -d "@$t" +%Y-%m-%dT%H:%M:%SZ 2>/dev/null || date -u +%Y-%m-%dT%H:%M:%SZ; else date -u +%Y-%m-%dT%H:%M:%SZ; fi; }
        started_at="$(iso_ts "${CI_PIPELINE_STARTED:-}")"
        finished_at="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
        tree="$(git rev-parse 'HEAD^{tree}')"
        artifacts="[]"
        artifact_digest="e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855"
        printf '{"schema":"faktor-woodpecker-lane/v2","lane":"docs","status":"passed","commit":"%s","tree":"%s","runner":{"os":"%s","arch":"%s","ci":"woodpecker","run_id":"%s"},"started_at":"%s","finished_at":"%s","commands_b64":"%s","commands_digest":"sha256:%s","artifacts":%s,"artifact_digest":"sha256:%s"}\n' \
          "$CI_COMMIT_SHA" "$tree" "$(uname -s | tr '[:upper:]' '[:lower:]')" "$(uname -m)" "${CI_PIPELINE_NUMBER:-0}" "$started_at" "$finished_at" "$cmds_b64" "$cmds_digest" "$artifacts" "$artifact_digest" > target/certification/lanes/docs.json
  - name: vscode
    image: node:24
    when:
      matrix:
        platform: linux/amd64
    volumes:
      - faktor-trusted-npm-cache:/root/.npm
    commands:
      - apt-get update && apt-get install -y --no-install-recommends unzip ca-certificates
      - cd apps/vscode
      - npm ci
      - npm run build
      - node scripts/selftest.mjs
      - npx --yes @vscode/vsce package --out faktor-ci.vsix
      - node scripts/verify-vsix.mjs faktor-ci.vsix --min-media-files 4
      - rm -rf /tmp/faktor-vsix && mkdir -p /tmp/faktor-vsix && unzip -q faktor-ci.vsix -d /tmp/faktor-vsix
      - node scripts/verify-vsix.mjs faktor-ci.vsix --extract-dir /tmp/faktor-vsix/extension
      - node scripts/selftest.mjs --packaged /tmp/faktor-vsix/extension
      - node scripts/verify-vsix.mjs faktor-ci.vsix --ide-load
      - cd "$CI_WORKSPACE"
      - |
        set -eu
        mkdir -p target/certification/lanes
        CMDS="$(cat <<'CMDS'
        apt-get update && apt-get install -y --no-install-recommends unzip ca-certificates
        cd apps/vscode
        npm ci
        npm run build
        node scripts/selftest.mjs
        npx --yes @vscode/vsce package --out faktor-ci.vsix
        node scripts/verify-vsix.mjs faktor-ci.vsix --min-media-files 4
        rm -rf /tmp/faktor-vsix && mkdir -p /tmp/faktor-vsix && unzip -q faktor-ci.vsix -d /tmp/faktor-vsix
        node scripts/verify-vsix.mjs faktor-ci.vsix --extract-dir /tmp/faktor-vsix/extension
        node scripts/selftest.mjs --packaged /tmp/faktor-vsix/extension
        node scripts/verify-vsix.mjs faktor-ci.vsix --ide-load
        cd "$CI_WORKSPACE"
        CMDS
        )"
        cmds_digest="sha256:$(printf '%s' "$CMDS" | sha256sum | cut -d' ' -f1)"
        cmds_b64="$(printf '%s' "$CMDS" | base64 | tr -d '\n')"
        iso_ts() { t="$1"; if [ -n "$t" ]; then date -u -r "$t" +%Y-%m-%dT%H:%M:%SZ 2>/dev/null || date -u -d "@$t" +%Y-%m-%dT%H:%M:%SZ 2>/dev/null || date -u +%Y-%m-%dT%H:%M:%SZ; else date -u +%Y-%m-%dT%H:%M:%SZ; fi; }
        started_at="$(iso_ts "${CI_PIPELINE_STARTED:-}")"
        finished_at="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
        tree="$(git rev-parse 'HEAD^{tree}')"
        artifact_path="apps/vscode/faktor-ci.vsix"
        artifact_sha="$(sha256sum "$artifact_path" | cut -d' ' -f1)"
        artifacts="[{\"path\":\"$artifact_path\",\"sha256\":\"sha256:$artifact_sha\"}]"
        artifact_digest="$(printf 'sha256:%s\t%s\n' "$artifact_sha" "$artifact_path" | sha256sum | cut -d' ' -f1)"
        printf '{"schema":"faktor-woodpecker-lane/v2","lane":"vscode","status":"passed","commit":"%s","tree":"%s","runner":{"os":"%s","arch":"%s","ci":"woodpecker","run_id":"%s"},"started_at":"%s","finished_at":"%s","commands_b64":"%s","commands_digest":"sha256:%s","artifacts":%s,"artifact_digest":"sha256:%s"}\n' \
          "$CI_COMMIT_SHA" "$tree" "$(uname -s | tr '[:upper:]' '[:lower:]')" "$(uname -m)" "${CI_PIPELINE_NUMBER:-0}" "$started_at" "$finished_at" "$cmds_b64" "$cmds_digest" "$artifacts" "$artifact_digest" > target/certification/lanes/vscode.json
  - name: jetbrains-build
    image: eclipse-temurin:17-jdk
    when:
      matrix:
        platform: linux/amd64
    volumes:
      - faktor-trusted-gradle-home:/root/.gradle
    commands:
      - cd apps/jetbrains
      - ./gradlew :frontend:buildPlugin --no-daemon --stacktrace
      - cd "$CI_WORKSPACE"
      - |
        set -eu
        mkdir -p target/certification/lanes
        CMDS="$(cat <<'CMDS'
        cd apps/jetbrains
        ./gradlew :frontend:buildPlugin --no-daemon --stacktrace
        cd "$CI_WORKSPACE"
        CMDS
        )"
        cmds_digest="sha256:$(printf '%s' "$CMDS" | sha256sum | cut -d' ' -f1)"
        cmds_b64="$(printf '%s' "$CMDS" | base64 | tr -d '\n')"
        iso_ts() { t="$1"; if [ -n "$t" ]; then date -u -r "$t" +%Y-%m-%dT%H:%M:%SZ 2>/dev/null || date -u -d "@$t" +%Y-%m-%dT%H:%M:%SZ 2>/dev/null || date -u +%Y-%m-%dT%H:%M:%SZ; else date -u +%Y-%m-%dT%H:%M:%SZ; fi; }
        started_at="$(iso_ts "${CI_PIPELINE_STARTED:-}")"
        finished_at="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
        tree="$(git rev-parse 'HEAD^{tree}')"
        artifact_path="$(find apps/jetbrains/frontend/build/distributions -maxdepth 1 -name '*.zip' -print -quit 2>/dev/null || true)"
        if [ -n "$artifact_path" ]; then
          artifact_sha="$(sha256sum "$artifact_path" | cut -d' ' -f1)"
          artifacts="[{\"path\":\"$artifact_path\",\"sha256\":\"sha256:$artifact_sha\"}]"
          artifact_digest="$(printf 'sha256:%s\t%s\n' "$artifact_sha" "$artifact_path" | sha256sum | cut -d' ' -f1)"
        else
          artifacts="[]"
          artifact_digest="e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855"
        fi
        printf '{"schema":"faktor-woodpecker-lane/v2","lane":"jetbrains-build","status":"passed","commit":"%s","tree":"%s","runner":{"os":"%s","arch":"%s","ci":"woodpecker","run_id":"%s"},"started_at":"%s","finished_at":"%s","commands_b64":"%s","commands_digest":"sha256:%s","artifacts":%s,"artifact_digest":"sha256:%s"}\n' \
          "$CI_COMMIT_SHA" "$tree" "$(uname -s | tr '[:upper:]' '[:lower:]')" "$(uname -m)" "${CI_PIPELINE_NUMBER:-0}" "$started_at" "$finished_at" "$cmds_b64" "$cmds_digest" "$artifacts" "$artifact_digest" > target/certification/lanes/jetbrains-build.json
  - name: jetbrains-smoke
    image: ubuntu:24.04
    when:
      matrix:
        platform: linux/amd64
    environment:
      RUST_BACKTRACE: "1"
      DEBIAN_FRONTEND: noninteractive
    volumes:
      - faktor-trusted-cargo-registry:/usr/local/cargo/registry
      - faktor-trusted-cargo-git:/usr/local/cargo/git
    commands:
      - apt-get update && apt-get install -y --no-install-recommends build-essential curl pkg-config libssl-dev kotlin openjdk-11-jre-headless ca-certificates
      - |
        set -eu
        curl --proto '=https' --tlsv1.2 -fsSLo /tmp/rustup-init https://static.rust-lang.org/rustup/archive/1.28.2/x86_64-unknown-linux-gnu/rustup-init
        printf '%s  %s\n' 20a06e644b0d9bd2fbdbfd52d42540bdde820ea7df86e92e533c073da0cdd43c /tmp/rustup-init | sha256sum -c -
        chmod +x /tmp/rustup-init
        /tmp/rustup-init -y --profile minimal --default-toolchain 1.98.0
      - . "$HOME/.cargo/env"
      - bash apps/jetbrains/compile-and-smoke.sh
      - |
        set -eu
        mkdir -p target/certification/lanes
        CMDS="$(cat <<'CMDS'
        apt-get update && apt-get install -y --no-install-recommends build-essential curl pkg-config libssl-dev kotlin openjdk-11-jre-headless ca-certificates
        set -eu
        curl --proto '=https' --tlsv1.2 -fsSLo /tmp/rustup-init https://static.rust-lang.org/rustup/archive/1.28.2/x86_64-unknown-linux-gnu/rustup-init
        printf '%s  %s\n' 20a06e644b0d9bd2fbdbfd52d42540bdde820ea7df86e92e533c073da0cdd43c /tmp/rustup-init | sha256sum -c -
        chmod +x /tmp/rustup-init
        /tmp/rustup-init -y --profile minimal --default-toolchain 1.98.0
        . "$HOME/.cargo/env"
        bash apps/jetbrains/compile-and-smoke.sh
        CMDS
        )"
        cmds_digest="sha256:$(printf '%s' "$CMDS" | sha256sum | cut -d' ' -f1)"
        cmds_b64="$(printf '%s' "$CMDS" | base64 | tr -d '\n')"
        iso_ts() { t="$1"; if [ -n "$t" ]; then date -u -r "$t" +%Y-%m-%dT%H:%M:%SZ 2>/dev/null || date -u -d "@$t" +%Y-%m-%dT%H:%M:%SZ 2>/dev/null || date -u +%Y-%m-%dT%H:%M:%SZ; else date -u +%Y-%m-%dT%H:%M:%SZ; fi; }
        started_at="$(iso_ts "${CI_PIPELINE_STARTED:-}")"
        finished_at="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
        tree="$(git rev-parse 'HEAD^{tree}')"
        artifacts="[]"
        artifact_digest="e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855"
        printf '{"schema":"faktor-woodpecker-lane/v2","lane":"jetbrains-smoke","status":"passed","commit":"%s","tree":"%s","runner":{"os":"%s","arch":"%s","ci":"woodpecker","run_id":"%s"},"started_at":"%s","finished_at":"%s","commands_b64":"%s","commands_digest":"sha256:%s","artifacts":%s,"artifact_digest":"sha256:%s"}\n' \
          "$CI_COMMIT_SHA" "$tree" "$(uname -s | tr '[:upper:]' '[:lower:]')" "$(uname -m)" "${CI_PIPELINE_NUMBER:-0}" "$started_at" "$finished_at" "$cmds_b64" "$cmds_digest" "$artifacts" "$artifact_digest" > target/certification/lanes/jetbrains-smoke.json
  - name: perf
    image: rust:1.98
    depends_on: [linux, static, docs]
    when:
      matrix:
        platform: linux/amd64
      status: [success, failure]
    environment:
      RUST_BACKTRACE: "1"
      CARGO_TARGET_DIR: /woodpecker-cache/target
    volumes:
      - faktor-trusted-cargo-registry:/usr/local/cargo/registry
      - faktor-trusted-cargo-git:/usr/local/cargo/git
      - faktor-trusted-target-perf:/woodpecker-cache/target
    commands:
      - cargo test -p faktor-tests-performance --release -- --ignored
      - |
        set -eu
        mkdir -p target/certification/lanes
        CMDS="$(cat <<'CMDS'
        cargo test -p faktor-tests-performance --release -- --ignored
        CMDS
        )"
        cmds_digest="sha256:$(printf '%s' "$CMDS" | sha256sum | cut -d' ' -f1)"
        cmds_b64="$(printf '%s' "$CMDS" | base64 | tr -d '\n')"
        iso_ts() { t="$1"; if [ -n "$t" ]; then date -u -r "$t" +%Y-%m-%dT%H:%M:%SZ 2>/dev/null || date -u -d "@$t" +%Y-%m-%dT%H:%M:%SZ 2>/dev/null || date -u +%Y-%m-%dT%H:%M:%SZ; else date -u +%Y-%m-%dT%H:%M:%SZ; fi; }
        started_at="$(iso_ts "${CI_PIPELINE_STARTED:-}")"
        finished_at="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
        tree="$(git rev-parse 'HEAD^{tree}')"
        artifacts="[]"
        artifact_digest="e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855"
        printf '{"schema":"faktor-woodpecker-lane/v2","lane":"perf","status":"passed","commit":"%s","tree":"%s","runner":{"os":"%s","arch":"%s","ci":"woodpecker","run_id":"%s"},"started_at":"%s","finished_at":"%s","commands_b64":"%s","commands_digest":"sha256:%s","artifacts":%s,"artifact_digest":"sha256:%s"}\n' \
          "$CI_COMMIT_SHA" "$tree" "$(uname -s | tr '[:upper:]' '[:lower:]')" "$(uname -m)" "${CI_PIPELINE_NUMBER:-0}" "$started_at" "$finished_at" "$cmds_b64" "$cmds_digest" "$artifacts" "$artifact_digest" > target/certification/lanes/perf.json
  - name: darwin-check
    image: bash
    when:
      matrix:
        platform: darwin/arm64
    commands:
      - cargo check --workspace
      - |
        set -eu
        mkdir -p target/certification/lanes
        CMDS="$(cat <<'CMDS'
        cargo check --workspace
        CMDS
        )"
        cmds_digest="sha256:$(printf '%s' "$CMDS" | shasum -a 256 | cut -d' ' -f1)"
        cmds_b64="$(printf '%s' "$CMDS" | base64 | tr -d '\n')"
        iso_ts() { t="$1"; if [ -n "$t" ]; then date -u -r "$t" +%Y-%m-%dT%H:%M:%SZ 2>/dev/null || date -u -d "@$t" +%Y-%m-%dT%H:%M:%SZ 2>/dev/null || date -u +%Y-%m-%dT%H:%M:%SZ; else date -u +%Y-%m-%dT%H:%M:%SZ; fi; }
        started_at="$(iso_ts "${CI_PIPELINE_STARTED:-}")"
        finished_at="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
        tree="$(git rev-parse 'HEAD^{tree}')"
        artifacts="[]"
        artifact_digest="e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855"
        printf '{"schema":"faktor-woodpecker-lane/v2","lane":"darwin-check","status":"passed","commit":"%s","tree":"%s","runner":{"os":"%s","arch":"%s","ci":"woodpecker","run_id":"%s"},"started_at":"%s","finished_at":"%s","commands_b64":"%s","commands_digest":"sha256:%s","artifacts":%s,"artifact_digest":"sha256:%s"}\n' \
          "$CI_COMMIT_SHA" "$tree" "$(uname -s | tr '[:upper:]' '[:lower:]')" "$(uname -m)" "${CI_PIPELINE_NUMBER:-0}" "$started_at" "$finished_at" "$cmds_b64" "$cmds_digest" "$artifacts" "$artifact_digest" > target/certification/lanes/darwin-check.json
  - name: darwin-test
    image: bash
    when:
      matrix:
        platform: darwin/arm64
    commands:
      - cargo test --workspace
      - |
        set -eu
        mkdir -p target/certification/lanes
        CMDS="$(cat <<'CMDS'
        cargo test --workspace
        CMDS
        )"
        cmds_digest="sha256:$(printf '%s' "$CMDS" | shasum -a 256 | cut -d' ' -f1)"
        cmds_b64="$(printf '%s' "$CMDS" | base64 | tr -d '\n')"
        iso_ts() { t="$1"; if [ -n "$t" ]; then date -u -r "$t" +%Y-%m-%dT%H:%M:%SZ 2>/dev/null || date -u -d "@$t" +%Y-%m-%dT%H:%M:%SZ 2>/dev/null || date -u +%Y-%m-%dT%H:%M:%SZ; else date -u +%Y-%m-%dT%H:%M:%SZ; fi; }
        started_at="$(iso_ts "${CI_PIPELINE_STARTED:-}")"
        finished_at="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
        tree="$(git rev-parse 'HEAD^{tree}')"
        artifacts="[]"
        artifact_digest="e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855"
        printf '{"schema":"faktor-woodpecker-lane/v2","lane":"darwin-test","status":"passed","commit":"%s","tree":"%s","runner":{"os":"%s","arch":"%s","ci":"woodpecker","run_id":"%s"},"started_at":"%s","finished_at":"%s","commands_b64":"%s","commands_digest":"sha256:%s","artifacts":%s,"artifact_digest":"sha256:%s"}\n' \
          "$CI_COMMIT_SHA" "$tree" "$(uname -s | tr '[:upper:]' '[:lower:]')" "$(uname -m)" "${CI_PIPELINE_NUMBER:-0}" "$started_at" "$finished_at" "$cmds_b64" "$cmds_digest" "$artifacts" "$artifact_digest" > target/certification/lanes/darwin-test.json
  - name: darwin-doctor
    image: bash
    when:
      matrix:
        platform: darwin/arm64
    commands:
      - cargo run -p faktor-cli -- doctor --data-dir /tmp/kp-ci
      - |
        set -eu
        mkdir -p target/certification/lanes
        CMDS="$(cat <<'CMDS'
        cargo run -p faktor-cli -- doctor --data-dir /tmp/kp-ci
        CMDS
        )"
        cmds_digest="sha256:$(printf '%s' "$CMDS" | shasum -a 256 | cut -d' ' -f1)"
        cmds_b64="$(printf '%s' "$CMDS" | base64 | tr -d '\n')"
        iso_ts() { t="$1"; if [ -n "$t" ]; then date -u -r "$t" +%Y-%m-%dT%H:%M:%SZ 2>/dev/null || date -u -d "@$t" +%Y-%m-%dT%H:%M:%SZ 2>/dev/null || date -u +%Y-%m-%dT%H:%M:%SZ; else date -u +%Y-%m-%dT%H:%M:%SZ; fi; }
        started_at="$(iso_ts "${CI_PIPELINE_STARTED:-}")"
        finished_at="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
        tree="$(git rev-parse 'HEAD^{tree}')"
        artifacts="[]"
        artifact_digest="e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855"
        printf '{"schema":"faktor-woodpecker-lane/v2","lane":"darwin-doctor","status":"passed","commit":"%s","tree":"%s","runner":{"os":"%s","arch":"%s","ci":"woodpecker","run_id":"%s"},"started_at":"%s","finished_at":"%s","commands_b64":"%s","commands_digest":"sha256:%s","artifacts":%s,"artifact_digest":"sha256:%s"}\n' \
          "$CI_COMMIT_SHA" "$tree" "$(uname -s | tr '[:upper:]' '[:lower:]')" "$(uname -m)" "${CI_PIPELINE_NUMBER:-0}" "$started_at" "$finished_at" "$cmds_b64" "$cmds_digest" "$artifacts" "$artifact_digest" > target/certification/lanes/darwin-doctor.json
  - name: windows-check
    image: powershell
    when:
      matrix:
        platform: windows/amd64
    commands:
      - cargo check --workspace
      - |
        $ErrorActionPreference = "Stop"
        New-Item -ItemType Directory -Force -Path target/certification/lanes | Out-Null
        $cmds = "cargo check --workspace"
        $bytes = [Text.Encoding]::UTF8.GetBytes($cmds)
        $sha256 = [Security.Cryptography.SHA256]::Create()
        $cmds_digest = ([BitConverter]::ToString($sha256.ComputeHash($bytes))).Replace('-','').ToLower()
        $cmds_b64 = [Convert]::ToBase64String($bytes)
        $tree = (git rev-parse 'HEAD^{tree}').Trim()
        $now = (Get-Date).ToUniversalTime().ToString("yyyy-MM-ddTHH:mm:ssZ")
        $marker = [ordered]@{
          schema = "faktor-woodpecker-lane/v2"
          lane = "windows-check"
          status = "passed"
          commit = "$env:CI_COMMIT_SHA"
          tree = $tree
          runner = [ordered]@{ os = "windows"; arch = "amd64"; ci = "woodpecker"; run_id = "$env:CI_PIPELINE_NUMBER" }
          started_at = $now
          finished_at = $now
          commands_b64 = $cmds_b64
          commands_digest = "sha256:$cmds_digest"
          artifacts = @()
          artifact_digest = "sha256:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855"
        } | ConvertTo-Json -Compress -Depth 6
        Set-Content -Path target/certification/lanes/windows-check.json -Value $marker
  - name: windows-test
    image: powershell
    when:
      matrix:
        platform: windows/amd64
    commands:
      - cargo test --workspace --exclude faktor-cli --exclude faktor-hooks --exclude faktor-tests-coding-benchmark --exclude faktor-tests-fuzz-seeds --exclude faktor-tests-performance
      - |
        $ErrorActionPreference = "Stop"
        New-Item -ItemType Directory -Force -Path target/certification/lanes | Out-Null
        $cmds = "cargo test --workspace --exclude faktor-cli --exclude faktor-hooks --exclude faktor-tests-coding-benchmark --exclude faktor-tests-fuzz-seeds --exclude faktor-tests-performance"
        $bytes = [Text.Encoding]::UTF8.GetBytes($cmds)
        $sha256 = [Security.Cryptography.SHA256]::Create()
        $cmds_digest = ([BitConverter]::ToString($sha256.ComputeHash($bytes))).Replace('-','').ToLower()
        $cmds_b64 = [Convert]::ToBase64String($bytes)
        $tree = (git rev-parse 'HEAD^{tree}').Trim()
        $now = (Get-Date).ToUniversalTime().ToString("yyyy-MM-ddTHH:mm:ssZ")
        $marker = [ordered]@{
          schema = "faktor-woodpecker-lane/v2"
          lane = "windows-test"
          status = "passed"
          commit = "$env:CI_COMMIT_SHA"
          tree = $tree
          runner = [ordered]@{ os = "windows"; arch = "amd64"; ci = "woodpecker"; run_id = "$env:CI_PIPELINE_NUMBER" }
          started_at = $now
          finished_at = $now
          commands_b64 = $cmds_b64
          commands_digest = "sha256:$cmds_digest"
          artifacts = @()
          artifact_digest = "sha256:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855"
        } | ConvertTo-Json -Compress -Depth 6
        Set-Content -Path target/certification/lanes/windows-test.json -Value $marker
  - name: certificate
    image: node:24
    depends_on:
      - linux
      - static
      - docs
      - vscode
      - jetbrains-build
      - jetbrains-smoke
      - perf
    when:
      matrix:
        platform: linux/amd64
      status: [success, failure]
    commands:
      - node scripts/certification/evidence.mjs verify-markers --workflow trusted --yaml-dir .woodpecker/trusted --lanes-dir target/certification/lanes --out target/certification/ci-certification.json --pipeline-status "$CI_PIPELINE_STATUS" --run-id "$CI_PIPELINE_NUMBER"
      - node scripts/certification/evidence.mjs write --kind ci_certification_trusted --status passed --from-markers target/certification/lanes --commands "woodpecker trusted linux workflow certificate"
  - name: certificate-darwin
    image: bash
    depends_on: [darwin-check, darwin-test, darwin-doctor]
    when:
      matrix:
        platform: darwin/arm64
      status: [success, failure]
    commands:
      - |
        set -u
        mkdir -p target/certification
        tree="$(git rev-parse 'HEAD^{tree}')"
        rc=0
        for lane in darwin-check darwin-test darwin-doctor; do
          f="target/certification/lanes/$lane.json"
          if [ -f "$f" ] && grep -q '"schema":"faktor-woodpecker-lane/v2"' "$f" && grep -q '"status":"passed"' "$f" && grep -q "\"commit\":\"$CI_COMMIT_SHA\"" "$f" && grep -q "\"tree\":\"$tree\"" "$f"; then
            echo "passed $lane"
          else
            echo "FAILED $lane (missing, failed, stale, wrong commit/tree, or wrong schema)"
            rc=1
          fi
        done
        if [ "$CI_PIPELINE_STATUS" != "success" ]; then
          echo "FAILED workflow status=$CI_PIPELINE_STATUS"
          rc=1
        fi
        if [ "$rc" -eq 0 ]; then st=pass; else st=fail; fi
        printf '{"schema":"faktor-ci-certification/v2","platform":"darwin","workflow":"trusted","commit":"%s","tree":"%s","pipeline":"%s","status":"%s"}\n' \
          "$CI_COMMIT_SHA" "$tree" "$CI_PIPELINE_NUMBER" "$st" > target/certification/ci-certification-darwin.json
        echo "certification=$st platform=darwin"
        [ "$rc" -eq 0 ]
  - name: certificate-windows
    image: powershell
    depends_on: [windows-check, windows-test]
    when:
      matrix:
        platform: windows/amd64
      status: [success, failure]
    commands:
      - |
        $ErrorActionPreference = "Stop"
        New-Item -ItemType Directory -Force -Path target/certification | Out-Null
        $tree = (git rev-parse 'HEAD^{tree}').Trim()
        $rc = 0
        foreach ($lane in @("windows-check", "windows-test")) {
          $f = "target/certification/lanes/$lane.json"
          if ((Test-Path $f) -and (Select-String -Quiet -Path $f -Pattern '"schema":"faktor-woodpecker-lane/v2"') -and (Select-String -Quiet -Path $f -Pattern '"status":"passed"') -and (Select-String -Quiet -Path $f -Pattern ('"commit":"' + $env:CI_COMMIT_SHA + '"')) -and (Select-String -Quiet -Path $f -Pattern ('"tree":"' + $tree + '"'))) {
            Write-Host "passed $lane"
          } else {
            Write-Host "FAILED $lane (missing, failed, stale, wrong commit/tree, or wrong schema)"
            $rc = 1
          }
        }
        if ($env:CI_PIPELINE_STATUS -ne "success") {
          Write-Host "FAILED workflow status=$env:CI_PIPELINE_STATUS"
          $rc = 1
        }
        $st = if ($rc -eq 0) { "pass" } else { "fail" }
        $manifest = '{"schema":"faktor-ci-certification/v2","platform":"windows","workflow":"trusted","commit":"' + $env:CI_COMMIT_SHA + '","tree":"' + $tree + '","pipeline":"' + $env:CI_PIPELINE_NUMBER + '","status":"' + $st + '"}'
        Set-Content -Path target/certification/ci-certification-windows.json -Value $manifest
        Write-Host "certification=$st platform=windows"
        if ($rc -ne 0) { exit 1 }