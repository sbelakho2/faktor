# Planted violation: apt usage against live Debian repositories with no
# apt-residual/apt-pinned annotation. `scripts/check-ci-image-pins.sh --dir`
# must reject this file.
steps:
  - name: linux
    image: rust:1.98.0@sha256:620dbcd124499c59e2406d3741574b5c5838cf9eb9656f0c3a03948f79b02959 # rust:1.98.0 @ 2026-09-24
    commands:
      - apt-get update && apt-get install -y --no-install-recommends ca-certificates
