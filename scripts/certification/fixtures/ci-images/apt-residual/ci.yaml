# Planted violation: the legacy `apt-residual` escape must be rejected now
# (P2-D is closed: snapshot pinning or a digest-pinned CI image only).
# `scripts/check-ci-image-pins.sh --dir` must reject this file.
steps:
  - name: linux
    image: rust:1.98.0@sha256:620dbcd124499c59e2406d3741574b5c5838cf9eb9656f0c3a03948f79b02959 # rust:1.98.0 @ fixture
    commands:
      # apt-residual: legacy escape (fixture; must not be accepted)
      - apt-get update && apt-get install -y --no-install-recommends ca-certificates
